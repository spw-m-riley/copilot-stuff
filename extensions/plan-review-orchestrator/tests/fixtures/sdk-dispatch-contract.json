{
  "bannedLaunchPatterns": [
    "session.send(",
    "session.sendAndWait(",
    "session.rpc.agent.select",
    "modifiedPrompt"
  ],
  "forbiddenOnSubagentEndPatterns": [
    "additionalContext",
    "|| \"\""
  ]
}
