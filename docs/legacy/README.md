# Legacy Artifacts

Files in this directory exist only for historical and migration reference. They are **not** active configuration.

| File | Status | Replacement |
| --- | --- | --- |
| `coherence.schema.json` | Legacy compatibility schema for the older Coherence naming. | The active memory system is **Lore**. Config lives at `lore.json`; the active schema lives in the `extensions/lore` submodule. |

If you are looking for the active surface, start at the repo root or [`../../CONTEXT.md`](../../CONTEXT.md).
